if COND_REPLAY
  MAYBE_REPLAY = replay
else
  MAYBE_REPLAY =
endif

if COND_XP_MAPEDIT
  MAYBE_XP_MAPEDIT = mapedit
else
  MAYBE_XP_MAPEDIT =
endif

if COND_SERVER_ONLY
  MAYBE_STUFF =
else
  MAYBE_STUFF = client $(MAYBE_REPLAY) $(MAYBE_XP_MAPEDIT)
endif

SUBDIRS = common server $(MAYBE_STUFF)
