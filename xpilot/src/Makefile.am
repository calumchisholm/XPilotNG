if COND_REPLAY
  MAYBE_REPLAY = replay
else
  MAYBE_REPLAY =
endif  

if COND_SERVER_ONLY
  MAYBE_CLIENT_REPLAY =
else
  MAYBE_CLIENT_REPLAY = client $(MAYBE_REPLAY)
endif

SUBDIRS = common server $(MAYBE_CLIENT_REPLAY)
